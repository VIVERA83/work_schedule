from fastapi import FastAPI

# from store.product.brand.views import brand_route
# from store.product.category.views import category_route
# from store.product.product.views import product_route


def setup_routes(app: FastAPI):
    """Настройка Роутов приложения."""
    # app.include_router(brand_route)
    # app.include_router(category_route)
    # app.include_router(product_route)
